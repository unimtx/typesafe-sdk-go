package typesafe

const (
	// Version is this SDK's semantic version without the leading "v".
	Version = "0.1.1"
	// DefaultBaseURL is the default TypeSafe API root.
	DefaultBaseURL = "https://api.typesafe.ai"
	// DefaultModel is used when SystemOneRequest.Model is empty.
	DefaultModel = "jev-latest"
)
